services:
  mongodb:
    image: mongo:7
    container_name: resumeai-mongo
    ports:
      - "27017:27017"
    volumes:
      - resumeai_mongo_data:/data/db
    restart: unless-stopped

volumes:
  resumeai_mongo_data:
